# Single source of truth for dependencies is [project.dependencies] in
# pyproject.toml -- this file just installs the project so the two can never
# drift out of sync.
#
# For development, prefer:  pip install -e ".[dev]"
# For notebooks/app/data:   pip install -e ".[viz,app,data]"
-e .
